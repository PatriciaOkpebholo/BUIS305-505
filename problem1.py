#### collect two inputs from the user to calculate the area of a rectangle;
# you need to define at least two variables; collect the length and breadth;
# calculate the area and print the area of the rectangle as output


l = float(input('enter the length of a rectangle:'))
b = float(input('enter the breadth of a rectangle:'))
Area = l*b
print('Area of a rectangle is:%.2f' % Area)